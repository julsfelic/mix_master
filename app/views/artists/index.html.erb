<h1>All Artists</h1>

<% if @artists.empty? %>
  <h2>There are no artists registered</h2>
<% else %>
  <% @artists.each do |artist| %>
    <h2><%= link_to artist.name, artist %></h2>
  <% end %>
<% end %>

<%= link_to "New artist", new_artist_path %>
